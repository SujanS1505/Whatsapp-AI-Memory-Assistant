# WhatsApp Bot — Environment Variables
# Copy this file to .env and fill in the values

# The name for the local auth session (used to persist login state)
WHATSAPP_SESSION=whatsapp-ai-session

# URL of the FastAPI AI backend
BACKEND_API_URL=http://localhost:8000

# Timeout for backend API calls in milliseconds
API_TIMEOUT=30000
